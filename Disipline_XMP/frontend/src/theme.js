// Shared brand accent, used as the default for every page's `accent` prop.
// Drives interactive color across the app via the --ing-accent/--nav-accent
// CSS custom properties (active nav, buttons, checkboxes, sliders, focus rings).
export const ACCENT = '#2563eb'
